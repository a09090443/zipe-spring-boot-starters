# example

工具類別單元測試範例，示範 `base-spring-boot-starter` 中執行緒池工具的使用方式。

## 快速啟動

```bash
cd example
./mvnw test
```

## 說明

本範例包含執行緒池相關的測試案例（`ReadPhotoLocation`、`ThreadCallableTask`），用於驗證 `base-spring-boot-starter` 的 `ThreadPoolTaskExecutor` 行為。
