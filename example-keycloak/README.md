# example-keycloak

示範 `keycloak-spring-boot-starter` 嵌入式 Keycloak 伺服器的使用範例。

> **注意：** 此範例使用 Spring Boot **2.4.4** 與 **Java 11**。

## 快速啟動

```bash
cd example-keycloak
./mvnw spring-boot:run
```

## 說明

啟動後 Keycloak 管理介面可透過設定的埠號存取，無需另外安裝或部署 Keycloak 伺服器。
